% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_episode_types.R
\name{classify_episode_types}
\alias{classify_episode_types}
\title{Classify peritonitis episode types for many episodes at once}
\usage{
classify_episode_types(
  patient_id,
  infection_date,
  last_dose_antibiotic,
  organism_list,
  outcome = rep(NA_character_, length(patient_id)),
  outcome_date = rep(as.Date(NA), length(patient_id))
)
}
\arguments{
\item{patient_id}{Character. Patient identifier for each episode.}

\item{infection_date}{Date. Date each episode was diagnosed.}

\item{last_dose_antibiotic}{Date. Date of the last antibiotic dose for each
episode.}

\item{organism_list}{List, one element per episode, each itself a list of
that episode's organism name(s). Use \code{"negative"} for culture-negative.}

\item{outcome}{Character. Outcome of each episode, or \code{NA} when resolved.
Defaults to all \code{NA}.}

\item{outcome_date}{Date. Date of each outcome. Defaults to all \code{NA}.}
}
\value{
Character vector, the same length as the inputs: \code{"relapsing"},
\code{"recurrent"}, \code{"repeat"}, or \code{NA_character_} (first valid
episode for a patient, invalid row, or no ISPD category applies).
}
\description{
A vectorised counterpart to \code{\link[=get_episode_type]{get_episode_type()}}. Each row is first checked
exactly as \code{\link[=pd_infection]{pd_infection()}} would check it, by building a \code{pd_infection}
with \code{\link[=new_pd_infection]{new_pd_infection()}} and passing it through \code{validate_pd_infection()}.
Valid rows are then classified as \code{"relapsing"}, \code{"recurrent"} or \code{"repeat"}
(ISPD 2022 definitions) against the same patient's immediately preceding
valid episode. The classification loop runs in C++.
}
